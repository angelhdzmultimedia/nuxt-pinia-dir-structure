{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=0.0.0.0 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "rimraf node_modules .nuxt pnpm-lock.yaml"
  },
  "devDependencies": {
    "nuxt": "^3.3.1"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.7",
    "nuxt-quasar-ui": "^1.0.5",
    "pinia": "^2.0.33",
    "rimraf": "^4.4.0"
  }
}
